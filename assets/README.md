# Artwork contract

Every file ending in `-original.png` is an untouched user file. The app currently renders the
second-generation pet sheets, the notepad, the bottle and jug controls, and the drink-frame
sheet. CSS framing selects an existing drawing without tracing, cleaning, smoothing, or
regenerating it. Generated material may only be added later as a separate prop or effect layer.
